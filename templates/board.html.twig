{% extends "base.html.twig" %}

{% block title %}Board{% endblock %}

{% block pageType %}{% if user is null %}login{% else %}board{% endif %}{% endblock %}

{% block body %}
    {% if user is null %}
        {% include './partials/login.html.twig' %}
    {% elseif not user.verified %}
        {% include './partials/userMessage.html.twig' %}
    {% else %}
        {% include 'partials/navigation.html.twig' %}
        <div class="board-container">
            <!-- board content -->
        </div>
    {% endif %}
{% endblock %}

